{
  "name": "deverge-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix ./devergehub run dev",
    "build": "npm --prefix ./devergehub run build",
    "start": "npm --prefix ./devergehub run start",
    "lint": "npm --prefix ./devergehub run lint"
  }
}
